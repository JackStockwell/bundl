<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Bundles</title>
  <link rel="stylesheet" type="text/css" href="./css/style.css">
</head>

<body>
  <nav>
    {{#if logged_in}}
    <button data-open-modal class="btn">Logout</button>
    <dialog data-modal id="logout-modal">
      <h2>Are you sure you want to logout?</h2>
      <button class="btn" onclick="handleLogout(event)">Yes</button>
      <button data-close-modal>No</button>
    </dialog>
    {{else}}
    <button id="logout" class="btn">Login</button>
    {{/if}}
    <a href="/profile/{{user.username}}">Profile</a>
  </nav>

{{{body}}}

<footer>
</footer>

</body>
  <script src="./js/script.js"></script>
</html>